import user from "./modules/user.js";
import generate from "./modules/generate.js";

/**
 * url *
 * method
 * data
 * params get
 */

const api = {
	userApi: user,
	generateApi: generate,
};

export default api;
